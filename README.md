# 腕立て200

腕立て200は、日々の腕立てメニューをランダムに決めて記録するためのシンプルな静的Webアプリです。

ブラウザだけで動作し、当日の記録は `localStorage` に保存されます。

## アプリ概要

以下の基本メニューをランダムに抽選し、実施順と合計回数を記録します。

- 肩: 40回
- 大: 60回
- 中: 40回
- 小: 40回
- 零: 20回

合計で200回のメニューを完了することを目的としています。

## 主な機能

- 次に行う腕立て種目のランダム抽選
- 当日の実施済み種目、残り種目、総回数の表示
- 実施順ログの表示
- 今日の記録のコピー
- 今日の記録のリセット
- 抽選設定の切り替え
  - 出た種目を候補から外す
  - 同じ種目が連続したら引き直す
  - 一度出た目の確率を半分にする
- 日付ごとの `localStorage` 保存

## ファイル構成

```text
.
├── index.html   # アプリのHTML構造
├── app.js       # 抽選、記録、localStorage保存などのアプリロジック
└── styles.css   # 画面デザイン
```

## 起動方法

依存パッケージやビルド手順はありません。

ブラウザで `index.html` を直接開くことで動作します。

ローカルサーバーで確認する場合は、プロジェクト直下で以下を実行します。

```sh
python3 -m http.server 8000
```

その後、ブラウザで以下を開きます。

```text
http://localhost:8000
```

## 動作確認方法

ブラウザでアプリを開き、以下を確認します。

1. 「次を引く」を押すと種目が表示される
2. 残り種目、実施済み種目、総回数が更新される
3. 順番ログに抽選結果が追加される
4. 設定チェックボックスを切り替えても画面が正常に更新される
5. 「コピー」で今日の記録がクリップボードにコピーされる
6. リセットボタンで今日の記録を初期化できる
7. ページを再読み込みしても当日の記録が保持される
